
import { userRepository } from "../repositories/indexRepository.js";

const userLoginService = async ({ userEmail, userPassword }) => {
  
};

const userRegisterService = async (userData) => {
  
};

const userChangePasswordService = async ({ userEmail, oldPassword, newPassword }) => {
  
};

const userUpdateProfileService = async (userData) => {
  
};

export {
  userLoginService,
  userRegisterService,
  userChangePasswordService,
  userUpdateProfileService
};
